'use client';

import LoginPage from '@/components/client/admin/LoginPage';

// This is a standalone page that does not use the admin layout
export default function AdminLoginPage() {
  return <LoginPage />;
}